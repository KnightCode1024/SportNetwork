services:
  postgres_sport_network:
    container_name: postgres_sport_network
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data_sport_network:/var/lib/postgresql/data
    networks:
      - sport_network_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 5

  rabbitmq_sport_network:
    container_name: rabbitmq_sport_network
    image: rabbitmq:4-management-alpine
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    volumes:
      - rabbitmq_data_sport_network:/var/lib/rabbitmq
    networks:
      - sport_network_net
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis_sport_network:
    container_name: redis_sport_network
    image: redis:8-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data_sport_network:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - sport_network_net

  minio_sport_network:
    container_name: minio_sport_network
    image: quay.io/minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      - MINIO_ROOT_USER=${S3_ACCESS_KEY}
      - MINIO_ROOT_PASSWORD=${S3_SECRET_KEY}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data_sport_network:/data
    networks:
      sport_network_net:
        aliases:
          - minio
  
  backend_sport_network:
    &backend
    container_name: backend_sport_network
    build: .
    restart: always
    volumes:
      - .:/backend
      - ./certs:/backend/certs:ro
      - ./.env:/backend/.env:ro
    working_dir: /backend
    environment:
      - PYTHONPATH=src
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_HOST=${DB_HOST}
      - DB_PORT=${DB_PORT}
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - APP_HOST=${APP_HOST}
      - APP_PORT=${APP_PORT}
    command: sh -c "alembic upgrade head && uvicorn sport_network_api.main:app --host ${APP_HOST} --port ${APP_PORT} --reload"
    ports:
      - "8001:8001"
    depends_on:
      postgres_sport_network:
        condition: service_healthy
      rabbitmq_sport_network:
        condition: service_healthy
      redis_sport_network:
        condition: service_healthy
      minio_sport_network:
        condition: service_started
    networks:
      - sport_network_net

  worker_sport_network:
    <<: *backend
    container_name: worker_sport_network
    ports: []
    command: taskiq worker sport_network_api.infrastructure.taskiq_broker:broker --workers 1 --fs-discover --tasks-pattern **/tasks/*.py

networks:
  sport_network_net:
    driver: bridge

volumes:
  postgres_data_sport_network:
  rabbitmq_data_sport_network:
  redis_data_sport_network:
  minio_data_sport_network:
